# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0               # async HTTP client used by tool adapters

# ── Configuration & validation ────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1    # reads .env via BaseSettings (used in app/config.py)
python-dotenv==1.0.1

# ── AI provider ───────────────────────────────────────────────────────────────
groq==0.25.0                 # Groq SDK


# ── Observability ─────────────────────────────────────────────────────────────
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0
redis==5.0.4
opentelemetry-exporter-otlp==1.15.0
opentelemetry-exporter-jaeger==1.15.0
opentelemetry-instrumentation-httpx==0.45b0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0               # TestClient for FastAPI uses httpx under the hood
fakeredis==2.23.0           # In-memory Redis for testing (used in tests/test_observability.py)